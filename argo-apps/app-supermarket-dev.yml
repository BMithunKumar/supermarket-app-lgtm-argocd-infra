apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: supermarket-dev
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/BMithunKumar/supermarket-app-lgtm-argocd-infra.git'
    targetRevision: HEAD
    path: helm/super-market-app
    helm:
      valueFiles:
        - values.dev.yaml
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: market-dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true